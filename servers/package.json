{
  "name": "sql-server-mcp",
  "version": "1.0.0",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "bundle": "esbuild src/minimal-mcp-server.ts --bundle --platform=node --format=cjs --target=node18 --outfile=dist/minimal-mcp-server.js",
    "build": "npm run typecheck && npm run bundle",
    "smoke": "node smoke-test.mjs",
    "start": "node dist/minimal-mcp-server.js",
    "dev": "tsx watch src/minimal-mcp-server.ts",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@azure/identity": "^4.13.1",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.4.5",
    "mssql": "^11.0.1"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.5",
    "@types/node": "^20.11.17",
    "esbuild": "^0.28.0",
    "rimraf": "^5.0.5",
    "tsx": "^4.7.1",
    "typescript": "^5.3.3"
  }
}
