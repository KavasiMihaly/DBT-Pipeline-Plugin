# Issue Tracker — dbt-pipeline-toolkit

Single source of truth for every known bug, empirical verification need, design gap, architectural risk, and enhancement in this plugin. This is NOT a backlog of planned features — see `_Plan/Backlog.md` for forward-looking work items. This file is for **problems we already know about**.

**Maintenance rules:** every new issue discovered during development or testing MUST be added here with a unique ID. Never silently fix an issue without an entry — create the entry, then mark it resolved in the same commit. See `CLAUDE.md` section "Issue Tracker Maintenance" for the full policy.

---

## Schema

| Column | Values |
|---|---|
| **ID** | Sequential `I-###` — never reuse IDs even after closing |
| **Title** | Short noun phrase describing the problem |
| **Category** | `empirical` (unverified claim needing fresh-install test), `bug` (known broken), `docs` (missing/incorrect documentation), `enhancement` (improvement to working feature), `risk` (known architectural concern) |
| **Severity** | `critical` (blocks shipping), `high` (significant impact), `medium` (workaround exists), `low` (nice to have) |
| **Status** | `open`, `in-progress`, `blocked` (waiting on external), `resolved` (fixed, awaiting verification), `closed` (fixed and verified), `wontfix` |
| **Found** | Date the issue was identified (YYYY-MM-DD) |
| **Source** | Which finding, session, or commit surfaced it |
| **Blocker** | What's preventing resolution (if blocked) or next step (otherwise) |

---

## Open issues

### Empirical verification needs (high priority — blocks shipping claims)

| ID | Title | Category | Severity | Status | Found | Source | Blocker |
|---|---|---|---|---|---|---|---|
| I-001 | PreToolUse hook actually fires for background subagent Bash calls | empirical | critical | closed | 2026-04-14 | Finding 9 | Verified 2026-04-16: hook fires. Was broken by wrong output format (I-031). |
| I-002 | PreToolUse hook `permissionDecision: "allow"` actually skips the prompt in background context | empirical | critical | in-progress | 2026-04-14 | Finding 9; `_Plan/workshop-readiness-2026-05-24.md` Slice 1 | Hook was using wrong output format `hookSpecificOutput.permissionDecision` instead of `decision: approve`. Fixed in I-031. Needs re-test with corrected format. In scope for workshop-readiness Slice 1 — discovery dry run will surface whether the hook fires in Stage 2 / Stage 7 background spawns. |
| I-003 | Profile JSON files materialize in `1 - Documentation/data-profiles/` after Stage 2 | empirical | critical | closed | 2026-04-16 | Findings 7 + 9 | Verified 2026-04-16: profile JSONs created successfully on fresh install. Confirms `${CLAUDE_PLUGIN_ROOT}` substitution and env var remap work. |
| I-004 | `${CLAUDE_PLUGIN_ROOT}` is actually substituted inline in agent/skill markdown body at load time | empirical | high | closed | 2026-04-16 | Finding 7 | Verified 2026-04-16: profile JSONs were created by Stage 2, which means the data-profiler script path in agent.md resolved correctly via `${CLAUDE_PLUGIN_ROOT}` substitution. Confirmed for both agent and skill markdown body. |
| I-005 | Plugin subagents receive their own agent.md body content at spawn time (Issue #13627) | empirical | high | in-progress | 2026-04-14 | Finding 6 research; `_Plan/workshop-readiness-2026-05-24.md` Slice 1 | Test: spawn a specialist, ask it in its prompt "what role are you specialized in?" — if generic answer, body content is being dropped. Slice 1 dry run effectively tests this — the staging-builder fan-out at Stage 7 either honors its agent.md body (which contains the bare `python scripts/` paths from I-047) or it doesn't. Either outcome resolves the empirical question. |
| I-006 | Plugin subagents can access declared MCP tools (Issue #13605) | empirical | high | open | 2026-04-14 | Finding 6 research | Test: have a specialist try to call `sql-server-mcp:*` tools and see if they resolve |
| I-007 | Plugin subagents auto-load project `CLAUDE.md` at spawn time | empirical | medium | open | 2026-04-14 | Finding 9 Round 3 | Test: deploy a `CLAUDE.md` with a distinctive rule at Stage 5, spawn a specialist in Stage 7, verify the specialist sees the rule |
| I-008 | `userConfig` configuration broken at multiple levels | bug | high | open | 2026-04-16 | Finding 5 Problem B | Three layers of breakage: (1) Prompts don't fire at install (upstream [#39827](https://github.com/anthropics/claude-code/issues/39827), [#39455](https://github.com/anthropics/claude-code/issues/39455)). (2) `/plugin` → Configure options exists but typing `n` in any input field closes the dialog (interprets as "no"/cancel), making it unusable for values containing the letter n. (3) Only reliable path: our `configure.py` script or manual `settings.json` edit. Document `configure.py` as primary config method in README. |
| I-009 | `skills:` agent frontmatter preload actually injects skill content into the subagent context | empirical | medium | open | 2026-04-14 | Finding 8 | After namespace fix, we don't know whether preloading was contributing anything behaviorally — test by removing a specialist's `skills:` field and comparing quality of generated models |
| I-065 | Background subagents cannot read `${CLAUDE_PLUGIN_ROOT}/reference/*` style guides at spawn time | bug | high | in-progress | 2026-05-21 | Sibling Dataflow plugin N14 + cross-plugin audit; verified against changelog 2026-05-24 (no upstream mitigation found); `_Plan/workshop-readiness-2026-05-24.md` Pre-Slice-1 | Dataflow's N14 documents this empirically — background subagents could NOT read `${CLAUDE_PLUGIN_ROOT}/reference/risk-catalog.md` from inside a `Task(run_in_background: true)` call because plugin-cache paths are outside the working-directory sandbox. DBT builders all reference `reference/sql-style-guide.md` and `reference/examples/{staging,dimension,fact}-models.md` from their `agent.md` bodies. Verified 2026-05-24 against `anthropics/claude-code` CHANGELOG.md (v2.1.110-v2.1.150): **no entry expands background-subagent filesystem read scope**; closest match v2.1.117 "Fixed subagents with different model flagging file reads" is unrelated. The restriction holds. **Material impact on workshop**: I-058's strengthened style-guide guidance (unaliased literals break view wrapper) cannot reach background staging-builders at Stage 7 — so the I-058 guidance is silently ineffective, and the only protection against the unaliased-literal class is post-hoc Grep regression gates. **Fix (Dataflow N14 playbook)**: `dbt-project-initializer` copies `${CLAUDE_PLUGIN_ROOT}/reference/*` into the project at scaffold time (e.g. `1 - Documentation/reference/`); builder `agent.md` files reference project-local paths instead. Pre-Slice-1 task in workshop plan. ~1 hour. |
| I-068 | Re-test: agent frontmatter `hooks:` and `mcpServers:` on main-thread `--agent` invocation (orchestrator) | empirical | medium | open | 2026-05-22 | Claude Code changelog audit v2.1.116 + v2.1.117 (verified 2026-05-24 against `anthropics/claude-code` CHANGELOG.md) | Two confirmed upstream fixes: (a) **v2.1.116**: "Agent frontmatter `hooks:` fire when running main-thread agents"; (b) **v2.1.117**: "Agent frontmatter `mcpServers` are now loaded for main-thread agent sessions". **No changelog entry for `permissionMode` on main-thread** — the original 2026-05-22 row claimed v2.1.116/120/121/130 lifted all three restrictions; that was incorrect. `permissionMode` removal stands; call-site `mode: "acceptEdits"` is still required. Re-test scope reduced to: can the orchestrator declare `hooks:` and/or `mcpServers:` in its OWN frontmatter (not subagents) when invoked as `claude --agent ...`. Low workshop priority; defer. |
| I-069 | Re-test: `worktree.bgIsolation: 'none'` setting as I-064 mitigation | empirical | high | open | 2026-05-22 | Claude Code changelog audit v2.1.143 | Upstream added `worktree.bgIsolation: 'none'` which lets background sessions edit the main working copy directly, eliminating the entire worktree-force-removal-destroys-untracked-files class. Combined with v2.1.133 ("worktree cleanup no longer falls back to `rm -rf`") the I-064 risk surface is much narrower than when filed. Test paths: (a) reproduce the original I-064 risk under current Claude Code to confirm whether it's even still present; (b) if still present, set `worktree.bgIsolation: 'none'` in `.claude/settings.local.json` via `configure.py` and confirm parallel builders no longer use isolation; (c) if green, close I-064 with "use upstream setting" note instead of writing a commit-and-merge-back hook. |
| I-070 | ~~Re-test: `AskUserQuestion` in background subagent under v2.1.146~~ — **CLAIM INVALID** | empirical | high | wontfix | 2026-05-22 | Claude Code changelog audit v2.1.146 (verified 2026-05-24 against `anthropics/claude-code` CHANGELOG.md) | **v2.1.146 does not exist in the changelog**. The "Auto mode no longer suppresses `AskUserQuestion`" entry quoted in the original row was a fabrication or misread. Current head is v2.1.150; no entry between v2.1.117 and v2.1.150 references `AskUserQuestion` or subagent tool-availability changes. The SDK Limitations docs (cited in Dataflow plugin N15) remain authoritative: `AskUserQuestion` is hard-restricted from any subagent spawned via the Agent tool, regardless of foreground/background. This invalidates I-063's "resolved" status — see updated I-063 row. Workshop plan brings the three-sub-step parent-owned pattern fix into scope. |
| I-071 | ~~Re-test: `userConfig` optional-field handling under v2.1.119~~ — **CLAIM INVALID** | empirical | medium | wontfix | 2026-05-22 | Claude Code changelog audit v2.1.119 (verified 2026-05-24 against `anthropics/claude-code` CHANGELOG.md) | **v2.1.119 was actually `/config` settings (theme, editor mode, verbose, etc.) now persist** — unrelated to `userConfig` or MCP env-var optional-field handling. The original 2026-05-22 row misread the entry. No changelog entry between v2.1.110 and v2.1.150 addresses `userConfig` install-time prompts or `${user_config.*}` optional-field handling. I-008 remains fully open at its original severity. Workshop plan continues to sidestep I-008 by running `configure.py` directly. |

### Real bugs (open)

| ID | Title | Category | Severity | Status | Found | Source | Notes |
|---|---|---|---|---|---|---|---|
| I-010 | Orchestrator frontmatter `description:` field still uses 2-part invocation command | bug | high | resolved | 2026-04-14 | Self-audit; re-confirmed by plugin-review-2026-04-21.md A-002 | Updated `agents/dbt-pipeline-orchestrator/agent.md:11` to use 3-part `claude --agent dbt-pipeline-toolkit:dbt-pipeline-orchestrator:dbt-pipeline-orchestrator`. Severity upgraded from low to high because the description is the primary user-visible invocation hint and wrong form produces "agent not found" at the entry point. |
| I-011 | `skills/sql-connection/` has no `SKILL.md` — not a valid skill | bug | medium | resolved | 2026-04-16 | Finding 5 audit | Added SKILL.md and `scripts/configure.py` (connection setup with presets for azure/local/local-sql). Now registers as `dbt-pipeline-toolkit:sql-connection`. Also documents the shared `connect.py` library role. Fixes I-022. |

### Documentation gaps

| ID | Title | Category | Severity | Status | Found | Source | Notes |
|---|---|---|---|---|---|---|---|
| I-012 | Plugin README missing main-thread invocation instructions | docs | high | open | 2026-04-14 | Finding 4 | The plugin README must prominently explain that the orchestrator only works when launched via `claude --agent dbt-pipeline-toolkit:dbt-pipeline-orchestrator:dbt-pipeline-orchestrator "Build a pipeline"` — not via @mention, auto-delegation, or `/agents` picker. Users invoking it any other way will hit the "subagents cannot spawn subagents" wall. |
| I-013 | Plugin README missing fresh-install configuration instructions | docs | medium | open | 2026-04-14 | Finding 5 Problem B | Until I-008 is resolved, the README needs a section telling users to manually add `sql_server`, `sql_database`, `sql_auth_type`, etc. to their `settings.json` under `pluginConfigs.dbt-pipeline-toolkit.options` if the install-time prompts don't fire. |
| I-014 | SKILL.md files use `${CLAUDE_PLUGIN_ROOT}/skills/<name>/` instead of `${CLAUDE_SKILL_DIR}/` | docs | low | open | 2026-04-14 | Finding 8 open follow-up | Inside a SKILL.md, the more idiomatic form is `${CLAUDE_SKILL_DIR}/scripts/<file>.py` which doesn't hardcode the skill's own name. Not a correctness issue — the current `${CLAUDE_PLUGIN_ROOT}/skills/<skill>/scripts/<file>.py` form works fine. Purely a cleanliness improvement. |
| I-066 | "Bug-class audit" policy lives only in user memory, not in CLAUDE.md — future contributors won't apply it | docs | medium | open | 2026-05-21 | Agent memory `feedback_audit_bug_class.md` + cross-plugin audit | Rule from user feedback memory: "when finding a bug that matches a prior resolved pattern, grep the entire plugin first, file ONE issue covering the full scope, fix all instances in one pass." This has surfaced repeatedly during plugin development (I-047 14 occurrences of bare `scripts/` paths, I-021 grep for `$HOME/.claude/skills/`, I-018 SKILL.md flag-name mismatches, I-036 6 specialist agent.md examples) — every time the discoverer had to re-derive the rule. Codify in `CLAUDE.md` under a new "Bug-class audit policy" subsection within the Issue Tracker Maintenance section, and reference it from any future `enhancement` row that prescribes "grep the whole plugin." Companion backlog row drives the README of the rule. |
| I-067 | No documented user-facing path for corporate TLS interception (Norton, Zscaler, etc.) — Python skills using `requests`/`certifi` fail silently with cert errors | docs | medium | open | 2026-05-21 | Agent memory + sibling Dataflow N12 | Several skills make outbound HTTPS calls (`dbt deps` to fetch packages, `pip install` during scaffold, MCP server auth in Entra modes). Under corporate TLS interception (Norton on this user's machine, Zscaler at many enterprises), Python's bundled `certifi` cert bundle rejects the re-signed certificates — but the failure mode is a cryptic SSL error deep in a subprocess log, not a hint that the user is behind a corporate proxy. The Dataflow plugin documents N12 with a `Setup-CorpCertBundle.ps1` helper and a `REQUESTS_CA_BUNDLE` env var pattern. Mirror that here: ship a helper script under `skills/sql-connection/scripts/` (or a new `skills/corp-tls/`) that exports the Windows cert store, appends it to certifi's bundle, and writes the env var. Document the symptom and the fix prominently in the README troubleshooting section. |

### Architectural risks

| ID | Title | Category | Severity | Status | Found | Source | Notes |
|---|---|---|---|---|---|---|---|
| I-015 | Plugin delegation via `Task` is officially "Not Planned" but empirically works | risk | high | open | 2026-04-14 | Finding 6 | The core subagent delegation path this plugin depends on was explicitly closed as "Not Planned" in GitHub Issue #19276. Empirical testing shows it works today, but Anthropic could break it in any release without warning. Mitigation: keep `_Research/plugin-subagent-delegation.md` updated with any new signals, add a CI smoke test that runs the orchestrator end-to-end before every release. Fallback architecture documented: skill-orchestrator pattern that converts the orchestrator to a Skill instead of an Agent. |
| I-016 | `permissionMode` stripping and other plugin-agent restrictions could apply to more fields | risk | medium | open | 2026-04-14 | Finding 2 | The docs list `hooks`, `mcpServers`, `permissionMode` as stripped from plugin agent frontmatter, but other fields might also be silently ignored. Worth periodically auditing plugin agent behavior against standalone agent behavior to catch new restrictions. |
| I-064 | `hooks/remove-worktree.py` runs `git worktree remove --force` with no merge-back step; destroys untracked files written by background builders inside the worktree | risk | high | in-progress | 2026-05-21 | Sibling Dataflow plugin N8 + cross-plugin audit; **upstream mitigation added in v2.1.143** (verified 2026-05-24 against `anthropics/claude-code` CHANGELOG.md); `_Plan/workshop-readiness-2026-05-24.md` Pre-Slice-1 | The Dataflow plugin hit this as N8: builders reported `status: success`, then `git worktree remove --force` destroyed every untracked file they wrote. Dataflow's chosen fix was to drop worktree isolation entirely. **Better option for DBT plugin now available**: Claude Code v2.1.143 added `worktree.bgIsolation: "none"` setting which lets background sessions edit the main working copy directly — no isolation, no force-remove, no destroyed files. **Workshop fix**: have `configure.py` add `worktree.bgIsolation: "none"` to the merge of `.claude/settings.local.json` at Pre-Stage. This closes the entire I-064 class in ~10 minutes with one settings line. Eliminates the need for either the merge-back hook rewrite (fix path a) or the worktree-isolation removal (fix path b). Plugin hooks `create-worktree.py`/`remove-worktree.py` and `plugin.json` registrations can stay — they just don't fire when the setting is `'none'`. **Pre-Slice-1 task in workshop plan.** Note: I-069 empirical re-test row was filed before this row was updated; mark I-069 as effectively answered by this row's upstream-mitigation path. Cross-ref: I-032 (worktree-name collision), I-069 (upstream empirical re-test). |

### Enhancements (low priority — nice to have)

| ID | Title | Category | Severity | Status | Found | Source | Notes |
|---|---|---|---|---|---|---|---|
| I-017 | Add `--strict` flag to `analyze_coverage.py` to separate report from enforcement | enhancement | low | open | 2026-04-15 | Round 2.5 phantom-flag incident | Current design overloads `--target` as both reporting target and enforcement threshold (script exits 1 unconditionally when below target). A cleaner design: `--target <pct>` sets the reporting target, `--strict` enables the exit-1 enforcement. Defaults: `--target 80`, strict off. Eliminates the `--target 0` workaround I had to document for reporting-only calls. |
| I-018 | Add a CI/pre-commit check that verifies flag names referenced in SKILL.md match script argparse | enhancement | medium | open | 2026-04-15 | Round 2.5 phantom-flag incident | Grep every SKILL.md for `python .*\.py\s+--<flag>` patterns, extract flag names, then grep the corresponding script's argparse for those flags. Fail if any flag in the docs doesn't exist in the script. Catches the class of "LLM invented a plausible flag name" bugs I hit during the atomic-commands refactor. |
| I-019 | Audit hook allowlist regex patterns for adversarial filename false-positives | enhancement | medium | open | 2026-04-14 | Finding 9 open follow-up | The patterns use `.fullmatch` with liberal `.*` — worth confirming no pattern could unintentionally match a command outside the plugin's scope (e.g. `python /etc/scripts/shadow_reader.py` — current patterns require `/skills/<name>/scripts/<file>.py` but edge cases should be tested with a parametrized test file) |
| I-020 | Expand hook `permissionDecisionReason` to include the specific matched category | enhancement | low | open | 2026-04-14 | Finding 9 open follow-up | Currently the reason is generic ("all subcommands match the allowlist"). Adding the specific category (python-script / git / filesystem / pip) would make the plugin's auto-approvals more auditable in transcripts. |
| I-021 | Audit for other dev-path patterns beyond `$HOME/.claude/skills/` | enhancement | low | open | 2026-04-14 | Finding 7 open follow-up | The grep pass that found 183 `$HOME/.claude/skills/` occurrences should be repeated for adjacent patterns like `$HOME/.claude/plugins/`, `$HOME/.claude/hooks/`, `%USERPROFILE%\.claude\...` to catch any other dev-workflow leakage. |
| I-022 | Document `connect.py` library structure since it's not a real skill | enhancement | low | resolved | 2026-04-16 | Related to I-011 | Resolved by I-011 fix — `sql-connection` now has a SKILL.md that documents both the configure command and the shared library role of `connect.py`. |
| I-023 | Scripts use `Path(__file__)` to find project root — fails when installed as plugin | bug | critical | resolved | 2026-04-16 | Fresh-install test | `load_data.py` walked up from `__file__` (plugin cache dir) to find `2 - Source Files/`. On install, this resolves to `~/.claude/plugins/cache/...` which never contains the user's data. Fixed: CWD-first resolution in `load_data.py` and `query_sql_server.py`. Needs fresh-install verification. |
| I-024 | Data profiler doesn't detect or report date formats | bug | high | resolved | 2026-04-16 | Road Safety retrospective | `profile_data.py:179` called `pd.to_datetime()` with no format hint. Ambiguous dates (e.g., 01/02/2024) were silently misinterpreted. Fixed: tries both dayfirst=True/False, samples raw values to detect separator/field order, reports `date_format` metadata (pattern, ambiguity flag, sample value) in profile output. Needs fresh-install verification. |
| I-025 | Staging builder doesn't bracket-quote SQL Server reserved words | bug | high | resolved | 2026-04-16 | Road Safety retrospective | Generated staging SQL uses bare column names like `date`, `type`, `status`, `name`, `key`, `value` which are SQL Server reserved words. These fail at compile/run time. The agent needs a reserved word guard — either always bracket-quote all column refs `[column_name]`, or maintain a reserved word list and quote selectively. |
| I-026 | Staging builder can create duplicate sources.yml blocks | bug | medium | resolved | 2026-04-16 | Road Safety retrospective | When the staging builder runs multiple times or builds multiple models for the same source, it can create duplicate `sources:` blocks in YAML files. Per-model YAML files (already documented) mitigate this, but the agent needs explicit read-before-write guidance to check for existing source definitions before creating new ones. |
| I-027 | dbt-sqlserver EXEC() wrapper breaks double-quoted refs in views | bug | critical | resolved | 2026-04-16 | Road Safety retrospective | The `dbt-sqlserver` adapter wraps view creation in `EXEC(N'...')`, which means any double quotes inside the SQL body become nested string escaping. This breaks `{{ source() }}` refs that contain double quotes. **Workaround**: set `materialized: table` for staging models on SQL Server to bypass the EXEC wrapper entirely. The staging builder agent currently defaults to `materialized: view`. |
| I-028 | dbt-runner has no venv auto-discovery | bug | medium | resolved | 2026-04-16 | Road Safety retrospective | `run_dbt.py` assumes `dbt` is in PATH (line 69). On projects where dbt is installed in a venv (the standard pattern from `dbt-project-initializer`), the script fails with "dbt command not found" unless the venv is already activated. Need walk-up discovery for `venv/`, `.venv/`, or `3 - Data Pipeline/venv/` and auto-activation before subprocess call. |
| I-029 | SQL scripts default to localhost when Azure config is set via plugin | bug | high | resolved | 2026-04-16 | Road Safety retrospective + Stage 6 failure | Three fixes: (1) All `--server`/`--driver` argparse defaults now read from `os.environ.get('SQL_SERVER', 'localhost')` instead of hardcoded `'localhost'`. (2) All 4 SQL scripts (`load_data.py`, `profile_data.py`, `query_sql_server.py`, `connect.py`) now fall back to reading `.claude/settings.local.json` (written by `configure.py`) when env vars are missing. (3) Precedence: bare env > CLAUDE_PLUGIN_OPTION_* > settings.local.json > hardcoded default. |
| I-030 | Data profiler output lacks column_name_mapping (original → sanitized) | bug | medium | resolved | 2026-04-16 | Road Safety retrospective |
| I-032 | WorktreeCreate hook names every worktree `wt-unknown` — parallel collisions | bug | high | resolved | 2026-04-17 | Dimension builder parallel test |
| I-033 | Builder agents query `dbo` instead of `dbo_analytics` for mart validation | bug | high | resolved | 2026-04-17 | Dimension builder test blocker | `dbt_project.yml` sets `+schema: analytics` for marts, so dbt-sqlserver creates `dbo_analytics`. But builder agents validated against `dbo.dim_*` / `dbo.fct_*` which found nothing. Fixed: added schema documentation to staging-builder, dimension-builder, and fact-builder agents. Also fixed initializer to use `materialized: table` for staging (EXEC() fix). | `create-worktree.py` used `data.get("worktree_id", "wt-unknown")` as fallback when Claude Code doesn't pass an ID. Parallel agents all got the same path, causing git worktree add to fail. Fixed: generate `wt-{uuid4_hex[:8]}` as unique fallback. |
| I-031 | Bash approver hook uses wrong output JSON format — `hookSpecificOutput` instead of `decision` | bug | critical | resolved | 2026-04-16 | Fresh-install test | Hook output `{"hookSpecificOutput": {"permissionDecision": "allow"}}` but Claude Code expects `{"decision": "approve"}`. The `validate-dbt-structure.py` hook in the same plugin uses the correct format. Fixed: changed `_emit_allow()` to output `{"decision": "approve", "reason": "..."}`. Also added top-level try/except so hook never crashes. Root cause of I-001/I-002 empirical failures. | Profile JSON output uses original CSV column names. When data is loaded via sql-executor, column names are sanitized (spaces→underscores, lowercase, etc). The staging builder needs both names but the profile only has originals. Add a `column_name_mapping` dict to the profile JSON that maps original → sanitized names so downstream agents can reference the correct database columns. |
| I-034 | Architecture setup agent bypasses `dbt-project-initializer` skill and creates divergent folder structure | bug | high | resolved | 2026-04-19 | Fresh-install test on second machine | Three-way mismatch between script output, agent.md, and SKILL.md. Script creates `3 - Data Pipeline/{tests, macros, seeds, snapshots, analyses}/` subfolders (all referenced by `dbt_project.yml` path declarations); agent.md only documented `models/{staging,intermediate,marts}/`; SKILL.md missed `analyses/` and `1 - Documentation/data-profiles/`. Additionally the agent sometimes scaffolded folders directly via `mkdir`/`Write` instead of calling the skill, producing structures that broke `ref()` resolution and profiler writes. Fixes: (1) script now also creates `1 - Documentation/data-profiles/`, (2) agent.md adds a "MANDATORY: You MUST call the skill" section forbidding direct `mkdir`/`Write` scaffolding, (3) agent.md Step 4 now enforces per-folder atomic `ls` verification and escalation-on-miss, (4) canonical structure diagrams in agent.md and SKILL.md updated to match script output exactly. Needs fresh-install verification. |
| I-045 | `pbip-from-dbt` script could not read connection from the initializer's `project-config.yml` schema | bug | critical | resolved | 2026-04-19 | Deployment readiness test | Script's `parse_config_file` expected `database.server` / `database.name` at the top level of `project-config.yml`. The plugin's `dbt-project-initializer` actually writes `dbt.database.{target,server}` AND `sql_server.{default_server,default_database}` — a completely different schema. Empirical test confirmed: with a real initializer-generated `project-config.yml` and no explicit `--server`/`--database` flags (the orchestrator Stage 12 default invocation), the script exited 2 with "SQL server not specified". Would have failed 100% of the time on a fresh install. Fixes: (1) `parse_config_file` now reads all three shapes in priority order: `dbt.database.*` → `sql_server.default_*` → legacy `database.*`. Uses a `_first(...)` helper so the first non-empty value wins. (2) Added `read_plugin_settings_local(project_root)` that falls back to `.claude/settings.local.json` under `pluginConfigs.dbt-pipeline-toolkit.options.{sql_server,sql_database}` — the ground-truth connection source written by `configure.py` at Pre-Stage. (3) Connection resolution chain is now: CLI flag > project-config.yml > settings.local.json, with a specific error message listing all three sources when nothing resolves. (4) Updated PyYAML-absent fallback parser to handle the indent-aware YAML tree (`dbt` → `database` → `server`) instead of single-level `database:` block. Empirical verification: end-to-end run with real initializer-shape `project-config.yml` (no CLI args) now produces all 19 expected PBIP files with correct server/database/schema in generated TMDL. |
| I-044 | Integrate `pbip-from-dbt` skill into orchestrator Stage 12, output to `4 - Semantic Layer/` | enhancement | medium | resolved | 2026-04-19 | New skill delivery | New skill `skills/pbip-from-dbt/` generates an openable Power BI Project (PBIP) from a completed dbt pipeline by reading `pipeline-design.md` Section 11 (Created Objects Registry) and `project-config.yml`. Script validated: `--help` works, argparse clean, atomic invocation compatible with PreToolUse hook allowlist (generic `python <path>/skills/<name>/scripts/*.py` pattern matches automatically). Three sets of fixes applied: (A) SKILL.md hardening — removed non-standard `paths:` frontmatter field, replaced all hardcoded `~/.claude/skills/...` invocation examples with `${CLAUDE_PLUGIN_ROOT}/skills/pbip-from-dbt/scripts/build_pbip.py` (I-004 plugin learning), removed stale "managed in a separate plugin repo" note, rewrote Workflow Integration to show Stage 12 sequence. (B) Orchestrator Stage 12 — now runs the skill ONLY when validator reports `Overall status: Validated`; skipped with a recorded note when Status is `Build complete, coverage below target` / `Build Failed` / `No Pipeline Found`; non-zero exit from the script does NOT abort the pipeline (already validated) — recorded as INFO in Section 12 for manual re-run. Updated user-facing success summary to include the PBIP path or a manual-invocation command if scaffolding was skipped. (C) Output folder relocation — per user direction, `--output` switched from `5 - Report Building` to `4 - Semantic Layer` (PBIPs are semantic-model-centric; `5 - Report Building` is reserved for standalone reports). Updated folder descriptions across `initialize_project.py` dict, `data-project-CLAUDE.md` template, `dbt-project-initializer/SKILL.md`, and `dbt-architecture-setup/agent.md` structure diagram. |
| I-043 | Pipeline validator still produced a legacy standalone `validation-report-<date>.md` alongside Section 10 of `pipeline-design.md` | bug | medium | resolved | 2026-04-19 | User-raised | Agent had dual personality: Phase 6 (lines 175-219) wrote a full report to `1 - Documentation/validation-report-<date>.md`, while a single line (30) also said "write to Section 9" — wrong section number AND wrong deliverable. Communication patterns at lines 282-302 advertised the separate file. Rewrote agent.md to be Section-10-only (matching the orchestrator's Stage 11 contract): 3-step workflow (read context → atomic `dbt build --full-refresh` + coverage analyzer → write Section 10). Exact 4-block Section 10 schema defined (Build / Test coverage / Findings / Next step) with explicit status-mapping rules (Validated / Build complete, coverage below target / Build Failed / No Pipeline Found). Explicit "do NOT create any other file, do NOT touch any other section" constraint. Dropped unused skills (`sql-executor`, `data-profiler`, `sql-server-reader` — none needed for the actual contract) and non-standard `background: true` frontmatter field. Fixed Section 9→10 numbering and removed broken `Agents/reference/` path reference (I-035). Also updated orchestrator Stage 12 handoff summary and both process-map files to stop pointing at the deleted report file. Size: 325 → 175 lines (46% reduction). |
| I-042 | Test coverage analyzer reads only legacy `tests:` YAML key, ignoring `data_tests:` produced by the test-writer | bug | high | resolved | 2026-04-19 | User report | dbt v1.8+ renamed the schema YAML test key from `tests:` to `data_tests:` (to disambiguate from unit tests). Commit 4232289 refactored the plugin's test-writer to emit `data_tests:` across all models and examples. But `analyze_coverage.py` still only read the legacy `'tests'` key (lines 142 + 151), so every modern schema file looked untested — coverage reports showed 0% regardless of how many tests existed. That cascaded into Stage 10 where the test-writer uses this analyzer to gate "coverage >= 80%" and was forced to add more tests endlessly. Fix: `_parse_schema_file` now uses a new `_collect_tests(block)` helper that reads BOTH `data_tests` (preferred, dbt >= 1.8) and legacy `tests` keys at model and column levels — any block with at least one entry counts as tested, and the union of test-name types is recorded. Mixed files (both keys present during a migration) are handled cleanly. Functional test confirmed: legacy=2, modern=2, both=2, none=0. |
| I-041 | Worktree hook defaults to `main` when repo is on `master`; orchestrator then improvised compound commands to "fix" it | bug | high | resolved | 2026-04-19 | GP data testing — dbt-test-writer worktree failed | Two linked failures: (a) `hooks/create-worktree.py` line 45-47 called `git branch --show-current` and fell back to the hardcoded `"main"` default if that command returned empty (detached HEAD) or failed (git older than 2.22). When the repo was on `master`, `git worktree add --detach <path> main` failed with "invalid reference: main". (b) The main orchestrator runtime then tried to remediate by emitting compound commands like `cd <path> && git branch -m main` — violating the atomic-commands rule and stalling. Fixes: (1) hook — new `resolve_base_branch()` tries 4 detection methods in order: `rev-parse --abbrev-ref HEAD`, `branch --show-current`, `for-each-ref refs/heads/` enumeration (prefers main > master > first), and only finally the requested default. Never silently defaults to `main` when the repo is on `master`. Clearer error message when worktree add fails. (2) Orchestrator Stage 5 — added Step 3a: a single atomic `git branch -m main` immediately after `git init` to normalize whatever git's `init.defaultBranch` produced. Downstream code can now assume `main`. Explicitly forbids `git init -b main`, `git checkout -b main && ...`, `cd <path> && git branch -m main`, and any other compound alternative. (3) Orchestrator header — added a HARD RULE section right after the "Run as Main Agent" block listing every forbidden shell operator, the `cd <path> && <cmd>` anti-pattern specifically, and the reason (PreToolUse hook allowlist matches per-subcommand). |
| I-040 | `initialize_project.py` overwrites `.claude/settings.local.json`, wiping the connection config from `configure.py` | bug | critical | resolved | 2026-04-19 | GP data testing — connection string did not propagate to load script | Pre-Stage of orchestrator runs `configure.py` which WRITES `.claude/settings.local.json` with `pluginConfigs.dbt-pipeline-toolkit.options.sql_server` etc. (merging with any existing content — `configure.py:63-71` reads before writing). Stage 5 then ran `initialize_project.py` which UNCONDITIONALLY overwrote the same file with only the permissions block (line 1122: `write_text(generate_settings_local_json(), ...)`), deleting the entire `pluginConfigs` block. All downstream scripts (`load_data.py`, `profile_data.py`, `query_sql_server.py`, `connect.py`) use `pluginConfigs` as one of their fallback sources for SQL connection env vars when `CLAUDE_PLUGIN_OPTION_*` is not set — so Stage 6 load fell back to `localhost` and failed. Fix: (1) Renamed `generate_settings_local_json()` → `default_settings_local()` returning a dict, (2) added `merge_settings_local(settings_path)` that preserves every top-level key (especially `pluginConfigs`) and union-merges `permissions.allow`/`deny` with existing entries (dedupe, insertion-order preserved), (3) handles malformed existing JSON by backing up to `.bak` instead of silently clobbering, (4) call site now merges + writes with a log line that announces whether `pluginConfigs` was preserved. Orchestrator Stage 5 also gained a defensive post-scaffold `configure.py --test-only` re-verification step so any future regression is caught before Stage 6 fails. |
| I-039 | Headerless CSVs silently produce meaningless column names across the whole pipeline | bug | critical | resolved | 2026-04-19 | GP data testing | `pd.read_csv(path)` with the default `header='infer'` treats row 0 of a headerless CSV as column names. Profiler, BA, and staging-builder all trusted those names as meaningful, producing `stg_*` models where `column_1` was really `patient_id` and nobody knew. Three-layer fix: (1) Profiler — `load_csv()` now uses `csv.Sniffer` + a numeric-row-0 sanity override to detect header presence, reads with `header=None` when missing/ambiguous, uses synthetic `col_N` names, and emits a new `header` block (`status`, `verified`, `verified_by`, `verification_source`) plus a `missing_header_row` critical quality issue on every affected profile. SQL sources are auto-marked `verified: true, verified_by: "ddl"`. (2) Business-analyst — new mandatory **Step 1b** protocol: when any profile flags missing/ambiguous headers, BA must NOT proceed to the 5-question discovery; instead WebSearch for a published dictionary (QOF, HES, NHS extracts, etc.), then confirm column-by-column with the user via AskUserQuestion, then rewrite the profile JSON with verified names and flip `header.verified = true`. If the user answers "unknown," STOP and escalate. Explicit "no inference from filename/values/domain guesses" rules. (3) Staging-builder — added fail-fast gate: refuses to build when `header.status != "present"` or `header.verified != true`, returns a structured `blocked` JSON envelope. (4) Orchestrator Stage 2 — now pre-scans profiles, tells BA to verify before discovery, and halts cleanly (writes to Section 12, no retry) when BA returns with `headers_unverified`. |
| I-038 | Business analyst agent.md still documents a separate requirements document instead of Section 1 of pipeline-design.md | bug | medium | resolved | 2026-04-19 | User-raised | Agent had dual personality: lines 55-113 correctly wrote to `pipeline-design.md` Section 1, but lines 137-311 described a legacy "standalone mode" that produced a separate multi-section `requirements-*.md` file. The legacy mode was never invoked by the current orchestrator — it was dead code bloating every spawn. Rewrote agent.md to be pipeline-only: 3-step workflow (read profiles → one AskUserQuestion call with 6 questions → write Section 1), plus an optional Step 1a for enrichment via `WebSearch`/`WebFetch` (unfamiliar domains) and `sql-server-reader` skill (incremental SQL sources). Explicit "no other file, no other section" constraint kept. Tools retained after user feedback: `WebFetch`, `WebSearch`, `sql-server-reader` skill — positioned as optional aids that improve Step 2 option quality, not as workflow steps or substitute deliverables. Size: 470 → 183 lines (61% reduction). Also fixed broken reference to removed `Agents/reference/` path (from I-035 cleanup). |
| I-037 | Harden `.gitignore` ordering so installed tools cannot leak into git | enhancement | high | resolved | 2026-04-19 | User-raised safety concern | Current order was safe but fragile: `.gitignore` was written at step 4 of 7 inside `initialize_project.py`, and the orchestrator ran `git add -A` with no verification that the gitignore actually covered `.venv/`, `dbt_packages/`, `target/`, `logs/`. Three reinforcements: (1) moved `.gitignore` write to the TOP of the skill's file-generation sequence, before any file operation that could create artifacts; (2) hardened the `.gitignore` content with `venv/`/`env/` alt names, `.env`/`.env.*`/`*.pem`/`*.key`/`secrets.yml` credential patterns, test caches (`.pytest_cache/`, `.mypy_cache/`, `.ruff_cache/`, `.coverage`, `htmlcov/`), and explicit nested dbt paths; (3) added pre-`git init` verification and post-`git add -A` leak-check steps to the orchestrator Stage 5, using the native `Grep` tool for content checks and `git ls-files --cached --error-unmatch` for leak detection (both auto-approved by the hook allowlist). |
| I-036 | Documentation examples in 6 specialist agent.md files miss `mode: "acceptEdits"` and use bare (non-namespaced) subagent names | bug | medium | resolved | 2026-04-19 | Plugin-learning audit | Six "Usage" examples violated two plugin learnings simultaneously: (1) bare `subagent_type: "data-explorer"` instead of 3-part `dbt-pipeline-toolkit:data-explorer:data-explorer`, (2) missing `mode: "acceptEdits"` alongside `run_in_background: true`. Affected files: `data-explorer/agent.md:46`, `dbt-test-writer/agent.md:391`, `dbt-pipeline-validator/agent.md:45`, `dbt-staging-builder/agent.md:546`, `dbt-dimension-builder/agent.md:330`, `dbt-fact-builder/agent.md:389`. Not live call sites — orchestrator itself is clean — but the examples are the canonical copy-paste pattern any user/LLM/future orchestrator refactor references. Both fields added to all 6 examples. Live orchestrator Tasks audited: all 8 background spawns (Stages 1, 7, 8, 9, 10, 11) already correctly set `mode: "acceptEdits"`; foreground spawns (business-analyst Stage 2, architecture-setup Stage 5) correctly omit it. |
| I-035 | Remove vestigial `6 - Agentic Resources/` folder and renumber `7 - Data Exports/` → `6 - Data Exports/` | enhancement | medium | resolved | 2026-04-19 | Audit during I-034 investigation | Audit found `6 - Agentic Resources/` had zero consumers: (a) initializer copied reference files from `Agents/reference/` — a path that doesn't exist in the plugin (real path is `reference/` at plugin root), so the skill always fell through to placeholder-stub branch; (b) no agent workflow reads `6 - Agentic Resources/` content; (c) generated CLAUDE.md and sql-executor/dbt-runner SKILLs referenced non-existent `6 - Agentic Resources/Skills/` subfolders from pre-plugin era; (d) `project-config.yml` declared `agents:`/`skills:` folders there, both dead. Removed entirely and renumbered `7 - Data Exports/` → `6 - Data Exports/`. Files changed: `initialize_project.py` (folder dict, `create_agentic_resources()` removed, `project-config.yml` gen, `.gitignore` gen, README structure diagram), `reset_project.py` (added legacy folder names for backward-compat cleanup), `query_sql_server.py` (prefers new name, falls back to legacy), `templates/data-project-CLAUDE.md`, `agents/dbt-architecture-setup/agent.md` (diagram + Step 4 ls checks), `skills/dbt-project-initializer/SKILL.md`, `skills/sql-server-reader/SKILL.md`, `skills/data-profiler/SKILL.md`, `skills/sql-executor/SKILL.md`, `skills/dbt-runner/SKILL.md`. Legacy `7 - Data Exports/` still recognized by reset and reader for projects scaffolded before this change. Needs fresh-install verification. |
| I-046 | `sql-executor/load_data.py` silently corrupts headerless CSVs | bug | high | in-progress | 2026-04-20 | User question — audit of load_data.py; `_Plan/workshop-readiness-2026-05-24.md` Slice 2 (conditional) | `load_csv_to_table()` at `skills/sql-executor/scripts/load_data.py:378` and `:403` calls `pd.read_csv(...)` with default `header='infer'`. On headerless CSVs this consumes row 0 of real data as column names, the sanitizer mangles those values into lowercase identifiers, and **one row of data is permanently lost**. Dtype inference is also skewed because pandas never sees row 0 as data. Sister script `skills/data-profiler/scripts/profile_data.py:203-260` already detects header status with `csv.Sniffer` + numeric-ratio heuristic, writes `header.status` / `header.verified` / verified column names to the sibling profile JSON at `1 - Documentation/data-profiles/profile_<safe_name>.json`, and I-039 extended this to the BA + staging-builder chain. The loader was missed — it has no profile lookup, no `--no-header` / `--columns` flag, and no fail-fast gate matching the staging-builder gate from I-039. Three-layer fix: (1) `_find_sibling_profile(csv_path)` using the same `safe_name` normalization as the profiler (stem → replace `.` → `__`, ` ` → `_`, `-` → `_`, lowercase); (2) when `header.status != 'present'` AND `header.verified == true`, load with `header=None, names=[col['column_name'] for col in profile['columns']]`; (3) when `status != 'present'` AND `verified != true`, refuse to load with a message directing user to BA verification, unless `--force-raw-load` is explicitly passed. Also add `--no-header` and `--columns "a,b,c"` manual-override flags for cases where no profile exists. Cross-ref: I-039. |
| I-047 | Builder agents use bare `python scripts/<file>.py` paths that never resolve in user projects | bug | high | in-progress | 2026-04-20 | User report — staging builder skipped validation saying "dbt not installed"; `_Plan/workshop-readiness-2026-05-24.md` Slice 2 (mechanical audit + fix) | All 4 builder agents (`staging`, `dimension`, `fact`, `test-writer`) have example Bash commands in their `agent.md` bodies like `python scripts/run_dbt.py compile ...` (14 occurrences). The `dbt-project-initializer` never creates a `scripts/` folder in the user's project — so `python scripts/run_dbt.py ...` is guaranteed to fail with FileNotFoundError in every user project. Spawned subagents apparently follow their own `agent.md` body examples rather than the orchestrator's prompt (which correctly uses `${CLAUDE_PLUGIN_ROOT}/skills/dbt-runner/scripts/run_dbt.py`), hit FileNotFoundError, heuristically classify it as "dbt not installed", and skip validation. Incidentally this is a counter-proof for I-005 — body content IS being received and followed by spawned specialists. Same class of bug as Finding 7 in CLAUDE.md (2026-04-14 `$HOME/.claude/skills/` → `${CLAUDE_PLUGIN_ROOT}/skills/` audit), but escaped because bare `scripts/` has no `$HOME` prefix to grep for. Locations: `agents/dbt-staging-builder/agent.md:178,186-188,391,394,397,403,406`, `agents/dbt-dimension-builder/agent.md:224-226`, `agents/dbt-fact-builder/agent.md:223-225`, `agents/dbt-test-writer/agent.md:313`; plus 2 user-facing error-message references in `skills/dbt-docs-generator/scripts/generate_docs.py:116,142`. Fix: mechanical replacement — `scripts/run_dbt.py` → `${CLAUDE_PLUGIN_ROOT}/skills/dbt-runner/scripts/run_dbt.py`, `scripts/query_sql_server.py` → `${CLAUDE_PLUGIN_ROOT}/skills/sql-server-reader/scripts/query_sql_server.py`, `scripts/generate_docs.py` (in print statements) → `${CLAUDE_PLUGIN_ROOT}/skills/dbt-docs-generator/scripts/generate_docs.py`. Add a Grep-based regression check alongside the one suggested in I-021. |
| I-048 | `dbt_utils.date_spine()` emits nested CTEs that T-SQL rejects; plugin reference recommends it anyway | bug | high | open | 2026-04-21 | Orchestrator agent feedback during dim_date build | `dbt_utils.date_spine()` internally generates `WITH rawdata AS (WITH p0 AS (...), p1 AS (...) ...)` — a nested `WITH` clause that SQL Server rejects outright with "Incorrect syntax near the keyword 'with'". This is a T-SQL dialect limitation, not just an EXEC() wrapper issue (I-027), so `materialized='table'` does not help. The plugin's reference example `reference/examples/dimension-models.md:241-316` recommends using `{{ dbt_utils.date_spine(...) }}` for SQL Server `dim_date` models, and `skills/dbt-project-initializer/scripts/initialize_project.py:216` auto-installs `dbt_utils` — so every orchestrator-generated project that follows the reference verbatim hits this failure. Investigated option to swap for `calogica/dbt_date` but research (see session transcript 2026-04-21) confirmed it does NOT fix the problem: (a) package README says "no longer actively supported"; (b) never officially supported dbt-sqlserver (PRs #14, #32 abandoned); (c) `dbt_date.get_date_dimension()` internally calls `dbt_utils.date_spine()` through `get_base_dates()` — inherits the exact same nested-CTE failure. Chosen fix (option #2 — plugin-shipped T-SQL macro): (1) create `skills/dbt-project-initializer/templates/macros/date_spine.sql` with a T-SQL-native macro using 5-way cross-joined VALUES-based tallies (no WITH clause in the macro body, scales to ~273 years); (2) `initialize_project.py` copies it to `3 - Data Pipeline/macros/date_spine.sql` during scaffold; (3) `reference/examples/dimension-models.md` uses `{{ date_spine('day', start, end) }}` instead of `{{ dbt_utils.date_spine(...) }}`; (4) `agents/dbt-dimension-builder/agent.md` documents the rule: on SQL Server, never call `dbt_utils.date_spine` — use the plugin's `date_spine` instead. `dbt_utils` stays in `packages.yml` because `generate_surrogate_key` and other macros are still used. **2026-04-21 update:** gotcha re-surfaced on a different machine's fresh-install build despite the above fixes — indicating the dimension-builder-only guidance wasn't enough. Added a combined "dbt-sqlserver CREATE VIEW Wrapper Gotchas" section to `reference/sql-style-guide.md` (covering this plus I-058, I-059) and cross-referenced it from all 4 builder agents (staging, dimension, fact, test-writer). Still needs fresh-install verification that the broadened guidance prevents regression. |
| I-049 | Test-writer writes to Section 8 of pipeline-design.md, overwriting Semantic Layer Plan; orchestrator expects Section 9 | bug | critical | resolved | 2026-04-21 | Full-plugin audit `_Research/plugin-review-2026-04-21.md` finding A-001 | Hard contract break. Orchestrator (`agents/dbt-pipeline-orchestrator/agent.md:249-263`) drafts Section 8 as the Semantic Layer Plan with 5 subsections (8.1-8.5: schema topology, conformed keys, Mermaid ER diagram, semantic notes for tmdl-scaffold handoff). Orchestrator line 265 drafts Section 9 as Test Strategy. Orchestrator Stage 10 line 572: `Verify test-writer wrote Section 9`. But `agents/dbt-test-writer/agent.md:29` said: "write your test strategy summary to Section 8". Silent failure mode: test-writer overwrites the semantic layer content at Section 8, then validator at Stage 11 searches Section 9, finds nothing, degrades pipeline validation — and the semantic layer handoff to `pbip-from-dbt` at Stage 12 is also destroyed. Fix: one-line change in `agents/dbt-test-writer/agent.md:29` from "Section 8" to "Section 9". Verified no other Section 8 references exist in test-writer. |
| I-050 | `dbt-docs-generator` `--warn-error` flag documented in SKILL.md but not implemented in argparse | bug | critical | resolved | 2026-04-21 | Full-plugin audit `_Research/plugin-review-2026-04-21.md` finding S-001 | `skills/dbt-docs-generator/SKILL.md:401` shows `python ... generate_docs.py generate --warn-error` as a usage example, but the script's argparse (`scripts/generate_docs.py:337-360`) never defined `--warn-error`. Any user copy-pasting the example hits `unrecognized arguments: --warn-error`. Same class of bug as I-018 ("LLM invented a plausible flag name"). Fix: (1) added `--warn-error` to `generate_parser` and `all_parser` argparse blocks; (2) added `warn_error` param to `DBTDocsGenerator.generate()` signature; (3) wired it into the `dbt docs generate` command list (`cmd.extend(["--warn-error"])` after target); (4) updated both call sites (generate and all subcommands) to pass `args.warn_error` through. |
| I-051 | `dbt-docs-generator` `export` subcommand missing `--target` flag; asymmetric API | bug | high | resolved | 2026-04-21 | Full-plugin audit `_Research/plugin-review-2026-04-21.md` finding S-002 | Three of four subcommands (`generate`, `serve`, `all`) in `skills/dbt-docs-generator/scripts/generate_docs.py` (lines 337-355) define `--target`; `export` (lines 358-360) did not. `DBTDocsGenerator.__init__` uses `self.target` to locate the dbt `target-path` folder, so export from a non-default target was impossible without a workaround. Fix: added `export_parser.add_argument('--target', help='dbt target to use')` — one line, symmetric with peer subcommands. |
| I-057 | Semantic contract guardrails: Section 8 must be primary, approval gate must surface it, builders must enforce strict conformance | enhancement | high | resolved | 2026-04-21 | User feedback + workflow sense-check during plugin-review follow-up | The existing workflow (orchestrator drafts Sections 5-8 in Stage 3 as a coherent plan, builders execute prompts in Stages 7-9) is sound, but has 6 guardrail gaps allowing silent degradation: **W-1** `agent.md:91` frames Section 8 as "you draft after dims/facts are planned" — misleading, invites future refactor to treat Section 8 as derived; **W-2** Stage 4 approval summary (`agent.md:272-313`) shows physical tables but not the semantic model, so user never explicitly approves the user-facing contract; **W-3** Stages 8/9 merge JSON envelopes but don't gate on `status != success` or deviations, allowing silent partial builds; **W-4** dim/fact JSON envelopes report what WAS built, not whether build matched prompt parameters (no `conforms_to_plan` / `deviations[]` fields); **W-5** Stage 7 staging-builder prompt is vague (`Read pipeline-design.md for context`) vs. Stage 8's tight `Read Sections 1-5 first`; **W-6** Section 8.4 Mermaid template has placeholder dims (`dim_customer`, `dim_product`) that can leak if orchestrator fails to replace them in Stage 3. Fix: minimal-surface-area edits across 4 agent files — (1) reword Section 8 master-doc-structure comment to "user-facing semantic contract"; (2) add Semantic Model block at top of Stage 4 approval summary with schema topology + conformed keys + Mermaid; (3) add strict post-merge gate at Stages 8 and 9: scan all envelopes for non-success or deviations, HALT on any hit, log to Section 12, escalate via AskUserQuestion; (4) add `conforms_to_plan: bool` and `deviations: []` to dim-builder and fact-builder JSON envelopes, plus Step 0 body section instructing builder to verify build output against prompt parameters; (5) tighten Stage 7 staging-builder prompt to name specific sections; (6) add explicit Mermaid-replacement instruction in Stage 3. No stage reordering, no workflow restructure — builders still receive params via prompt (orchestrator remains single source of plan consistency); guardrails just catch deviations at the builder seam. Full end-to-end workflow documented in `_Documentation/pipeline-workflow.md`. |
| I-058 | Unaliased literal columns break CREATE VIEW wrapper with error 4511 | bug | high | in-progress | 2026-04-21 | Fresh-install build on different machine — user feedback; `_Plan/workshop-readiness-2026-05-24.md` Slice 2 (fresh-install verification + Grep regression gate if guidance insufficient) | T-SQL requires every view column to have a name. dbt-sqlserver wraps every non-table model in `create view {{ this }} as select * from (<model SQL>) _dbt_view_wrapper`, and the outer `select *` does not assign names, so any unaliased literal column (e.g. `select order_id, 1, null from ...`) in a model materialized as `view` or `ephemeral` fails compilation with `Msg 4511: CREATE VIEW failed because no column name was specified for column N`. Plugin templates don't emit bare literals, but LLM-scaffolded staging and intermediate models sometimes do — especially in `union all` branches, scaffolding stubs, and constant columns in CASE output. No explicit rule told the builders to always alias literals. Fix (applied 2026-04-21): (1) added "dbt-sqlserver CREATE VIEW Wrapper Gotchas" section to `reference/sql-style-guide.md` documenting the three related gotchas (this, I-048, I-059) with the shared root cause — the adapter's view wrapper has stricter T-SQL constraints than the raw model SQL; (2) cross-referenced that section from `agents/dbt-staging-builder/agent.md`, `agents/dbt-fact-builder/agent.md`, `agents/dbt-test-writer/agent.md`, and added a pointer in `agents/dbt-dimension-builder/agent.md`'s existing date_spine section. Still needs fresh-install verification that the guidance prevents regression. |
| I-059 | `OPTION (MAXRECURSION N)` is silently stripped by the CREATE VIEW wrapper, capping recursion at 100 rows | bug | high | in-progress | 2026-04-21 | Fresh-install build on different machine — user feedback; `_Plan/workshop-readiness-2026-05-24.md` Slice 3 (Stage 8-12 fallback dry run verifies guidance applied where relevant) | Query-level T-SQL hints like `OPTION (MAXRECURSION 500)` only bind to the outermost statement. When dbt-sqlserver wraps a `view`-materialized model in `select * from (<model SQL>) _dbt_view_wrapper`, the hint attaches to the wrapper's outer SELECT instead of the recursive CTE, and T-SQL silently caps recursion at the default 100 rows with no warning. Any model that uses a recursive CTE must be `materialized='table'` so the adapter emits `SELECT INTO` / `INSERT ... SELECT` instead of a view wrapper, preserving the hint on the outermost statement. Zero occurrences of `MAXRECURSION` exist in the plugin source — the symptom comes from LLM-scaffolded recursive calendar / hierarchy / bill-of-materials models that default to view materialization. Fix (applied 2026-04-21): same combined style-guide section as I-058 — "dbt-sqlserver CREATE VIEW Wrapper Gotchas" documents the rule "recursive CTEs or MAXRECURSION hints must be `materialized='table'`" with an example, and all 4 builder agents now cross-reference it. Still needs fresh-install verification. |
| I-060 | `reset_project.py` drops dim/fact tables from wrong schema (dbo_staging) instead of dbo_analytics | bug | critical | resolved | 2026-04-21 | Fresh-install dry-run, user feedback | Script CLI accepted `--schemas raw,dbo` — a single-dbt-schema model that doesn't match dbt-sqlserver's actual layout. `generate_drop_sql` then used the single `dbt_schema` value for both staging views AND dim/fact tables (lines 134, 144, 149). But dbt-sqlserver puts staging in `{target}_staging` (e.g. `dbo_staging`, from `+schema: staging`) and marts in `{target}_analytics` (e.g. `dbo_analytics`, from `+schema: analytics`). On a 22-table pipeline build, the user's dry-run showed `DROP TABLE [dbo_staging].[fct_*]` / `[dbo_staging].[dim_*]` — which silently succeeds (table not found is not an error for `DROP TABLE IF EXISTS`) but leaves every mart table orphaned in `dbo_analytics`. Next build would then hit name collisions. Fix (applied 2026-04-21): (1) split `dbt_schema` into separate `staging_schema` and `marts_schema` parameters in `generate_drop_sql` and its call site; (2) added `detect_schemas_from_dbt_project()` that regex-parses `dbt_project.yml` (`+schema:` under `staging:` / `marts:`) + `profiles.yml` (target schema) and composes final schema names as `{target}_{suffix}`; (3) added `resolve_schemas()` that layers CLI override > auto-detect > defaults (raw=raw, staging=dbo_staging, marts=dbo_analytics); (4) `--schemas` now accepts 1, 2, or 3 comma-separated values — legacy 2-value form (`raw,dbo`) still parses but emits a warning redirecting to the 3-value form; (5) updated orchestrator's reset command examples at `agents/dbt-pipeline-orchestrator/agent.md:784-799` to show the auto-detect path and the 3-value override. Regex uses `[ \t]` instead of `\s` to avoid crossing newlines between staging/marts blocks (initial attempt did exactly that and returned `analytics` for both). End-to-end tested on synthetic dbt_project.yml + registry — all 4 object types land in correct schemas. Still needs fresh-install verification on a real multi-environment project. |
| I-061 | `reset_project.py` produces `DROP TABLE [raw].[raw.epraccur]` — doubled schema prefix when registry contains schema-qualified names | bug | critical | resolved | 2026-04-21 | Fresh-install dry-run, user feedback | Orchestrator's Stage 6 template says `| raw_{source_name} | TABLE | Stage 6 |` (underscore prefix), but LLMs writing Section 11 in practice often use schema-qualified dot form `| raw.epraccur | TABLE | Stage 6 |`. The reset script took the registry entry at face value and wrapped it as `[raw].[<entry>]`, producing `[raw].[raw.epraccur]` — a non-existent table. Fix (applied 2026-04-21): added `normalize_object_name(name, raw_schema)` that strips schema-qualified dot prefix (`raw.epraccur` → `epraccur`, also handles `db.schema.table`). Every parsed name flows through normalization in both `parse_pipeline_design()` and `_fallback_parse()`. Also widened fallback regex to match both underscore (`raw_epraccur`) and dot (`raw.epraccur`) forms. Deliberately does NOT strip underscore prefix (`raw_epraccur` → `epraccur`) — that would break legitimate `raw_*`-named source tables. The orchestrator's underscore template inconsistency is tracked separately as I-062. Unit-tested 10 input variants including bare names, dot-qualified, underscore-prefixed, and three-part `db.schema.table`. End-to-end tested with synthetic pipeline-design.md — output SQL correctly uses `[raw].[epraccur]` not `[raw].[raw.epraccur]`. |
| I-062 | Orchestrator Section 11 registry template uses `raw_{source_name}` but loaded tables are bare `{source_name}` in `raw` schema | bug | medium | open | 2026-04-21 | Audit during I-060/I-061 fix | `agents/dbt-pipeline-orchestrator/agent.md:511` tells the orchestrator to write `| raw_{source_name} | TABLE | Stage 6 |` in the Created Objects Registry. But `skills/sql-executor/scripts/load_data.py:378` documents that the loaded table name is "without schema prefix" — i.e. actual DB tables are `raw.epraccur` (schema=`raw`, table=`epraccur`). LLMs handle this inconsistency in multiple ways: some follow the template literally (`raw_epraccur`), others use schema-qualified dot form (`raw.epraccur`), others write bare (`epraccur`). All three appear in practice. I-061's normalizer handles the dot form and bare form correctly; the underscore form is deliberately left intact to avoid breaking legitimate `raw_*`-named tables. Fix: update the orchestrator template at Stage 6 to use bare names — `\| {source_name} \| TABLE \| Stage 6 \|` — and add a note that the table lives in the `raw` schema (already implied by the "### Raw Tables (schema: raw)" section header at line 117). This aligns the registry with actual DB state and removes the template ambiguity that motivated part of I-061. Low-risk edit. No migration needed — existing registries with `raw_*` entries still work via manual reset if the user provides bare names, but a fresh rebuild under the fixed template produces clean entries. |
| I-072 | Stage 7 parallel staging-builder fan-out likely produces stray files outside assigned paths | bug | high | open | 2026-05-24 | Sibling Dataflow plugin N16 (2026-05-24 live-run on 7-dataflow workspace, 10 stray files + 2 stray directories across 14 builders) + cross-plugin audit | Dataflow's N16 documents: builder agents fanned out in parallel produce files outside their assigned notebook paths — build report markdown next to models, JSON envelopes written as files instead of returned in chat response, invented directories like `9 - Build Outputs/` and `7 - Data Exports/` that don't exist in scaffold, builder-specific notes dumped into `1 - Documentation/` (the orchestrator's master design doc namespace). Root cause is two-part: (a) builder agent.md bodies have instructions like "save documentation to..." that get interpreted as file-writes rather than chat-response content; (b) orchestrator prompts say "Return JSON envelope" — ambiguous "Return" interpreted as "write to file" by some models. DBT plugin's Stage 7 fans out N parallel staging-builders with identical structural setup; same class of pollution is likely. **Impact**: post-Stage-7 cleanup overhead in the orchestrator (which currently doesn't have it), confused user (extra files in project tree they didn't expect), risk of stray file fooling subsequent stages (e.g. test-writer reading a builder's stray report as a model definition). **Fix paths**: (1) audit `agents/dbt-staging-builder/agent.md`, `agents/dbt-dimension-builder/agent.md`, `agents/dbt-fact-builder/agent.md`, `agents/dbt-test-writer/agent.md` for any "save to..." / "write to..." / "create report at..." instructions that should be chat-response content; (2) tighten orchestrator Stage 7-10 prompts to say "Include this JSON envelope as the LAST block of your chat response — formatted as a fenced ```json``` block. DO NOT write the envelope to a file. DO NOT create any files outside the model paths specified above."; (3) add a pre-ship audit gate that greps every `agent.md` body for `.md` write instructions outside known-allowed paths. Workshop scope: items (1) and (2) only; ~30 min. Pre-Slice-1 in workshop plan. |
| I-063 | Business-analyst cannot invoke `AskUserQuestion` because subagents are hard-restricted from the tool at the SDK level | bug | critical | open | 2026-04-21 (re-opened 2026-05-24) | Originally: user report — multiple fresh-install builds. Re-opened: Dataflow plugin learning N15 + Claude Agent SDK Limitations docs; verified against changelog 2026-05-24 | **Re-opened 2026-05-24** after cross-plugin audit revealed the original "resolved" status was based on a wrong root-cause hypothesis. Original (2026-04-21) hypothesis: BA was using the wrong invocation shape. Fix applied: rewrote agent.md with the correct `questions[]`/`options[]`/`header`/`multiSelect` schema and reduced question count to 4. **True root cause (per Claude Agent SDK [user-input docs](https://code.claude.com/docs/en/agent-sdk/user-input.md) — Limitations section): `AskUserQuestion` is not currently available in subagents spawned via the Agent tool.** Listing the tool in `tools:` frontmatter has no effect. Foreground vs background is irrelevant. Verified against `anthropics/claude-code` CHANGELOG.md 2026-05-24: no entry between v2.1.110 and v2.1.150 lifts this restriction. (The original I-070 row claimed v2.1.146 added a fix; that entry does not exist — see updated I-070.) **The 2026-04-21 shape fix is preserved** but doesn't address the actual root cause; BA still silently degrades to plain-text questions on every spawn. **Workshop-critical**: Stage 1-2 is money shot #1; current behavior makes that demo moment unreliable. **Fix path** (Dataflow plugin's N15 playbook): three-sub-step parent-owned pattern — (2a) BA in `Mode: analyze` reads profiles, returns JSON envelope `1 - Documentation/discovery-questions.json` of applicable questions + options; (2b) Orchestrator (main session) calls `AskUserQuestion` with the envelope, writes answers to `1 - Documentation/discovery-answers.json`; (2c) BA in `Mode: write` consumes answers + profiles, writes Section 1 of `pipeline-design.md` deterministically. Both BA spawns run background; neither needs `AskUserQuestion`. Brought into scope by `_Plan/workshop-readiness-2026-05-24.md` as a pre-Slice-1 task. |

---

## Recently resolved (kept for historical context)

Resolved issues are kept in this file for ~30 days before being archived. Items with `closed` status (fixed AND verified on a fresh install) can be moved to `_Plan/Issues-Archive.md` after verification.

*(No resolved issues yet — this file was just created.)*

---

## Issue lifecycle

1. **Discovery** — a new problem is identified during development, testing, or research. Create a new `I-###` row immediately. Never leave undocumented problems in the codebase.
2. **Triage** — assign category and severity. If severity is `critical`, flag it in the next status message to the user so it's not missed.
3. **Work** — when someone starts on it, update status to `in-progress`. If the work requires a research doc, put it in `_Research/` and link from the `Source` column. If the work requires a plan, put it in `_Plan/<name>.md` and link from `Source`.
4. **Resolution** — when the code fix is committed, update status to `resolved`. Keep the issue open until it's verified.
5. **Verification** — after the fix is tested (usually on a fresh install), update status to `closed` and add a note about what was tested.
6. **Archive** — closed issues older than 30 days can be moved to `Issues-Archive.md` to keep this file tight.

## When an issue is NOT appropriate

Some things should NOT go in this tracker:

- **New features planned from scratch** — those go in `Backlog.md`. The issue tracker is for problems with existing behavior.
- **Conversation-level context** — "the user asked about X" is not an issue. Issues should be actionable technical work.
- **Questions for the user** — ask in conversation, don't file as issues.
- **Completed work with no outstanding concerns** — once a fix is verified, it doesn't need an ongoing tracker entry. Archive it.
